import React from "react";
import { Col, Row } from "./styled";
import { Button, Paper } from "@material-ui/core";

export class CreateCourseForm extends React.Component {
  render () {
    return (
      <Paper elevation={2}>
        <Col minHeight="600px" minWidth="400px" p={2}>
          {this.props.children}
          <Row mt="auto" ml="auto">
            <Button color="secondary" onClick={()=>{this.props.goBack();}}>CANCEL</Button>
            <Button color="primary" onClick={this.props.onSubmit}>CONFIRM</Button>
          </Row>
        </Col>
      </Paper>
    );
  }
}
